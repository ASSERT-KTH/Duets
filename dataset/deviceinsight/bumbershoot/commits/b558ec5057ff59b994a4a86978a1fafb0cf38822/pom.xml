<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" 
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>com.deviceinsight</groupId>
	<artifactId>bumbershoot</artifactId>
	<version>0.1.0</version>
	<packaging>jar</packaging>

	<name>bumbershoot</name>
	<description>continous deployment for helm umbrella charts</description>

	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>2.0.3.RELEASE</version>
		<relativePath />
		<!-- lookup parent from repository -->
	</parent>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
		<java.version>10</java.version>

		<spring-cloud.version>Finchley.RELEASE</spring-cloud.version>
		<guava.version>23.0</guava.version>
		<semver.version>0.9.0</semver.version>
		<commons-compress.version>1.17</commons-compress.version>

		<docker.plugin.version>0.26.0</docker.plugin.version>
		<docker.baseImage>openjdk:10-jdk</docker.baseImage>

		<helm.plugin.version>2.6</helm.plugin.version>
		<helm.swift.version>0.8.1</helm.swift.version>
	</properties>

	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.cloud</groupId>
			<artifactId>spring-cloud-starter-openfeign</artifactId>
		</dependency>
		<dependency>
			<groupId>com.squareup.okhttp3</groupId>
			<artifactId>okhttp</artifactId>
		</dependency>
		<dependency>
			<groupId>com.fasterxml.jackson.dataformat</groupId>
			<artifactId>jackson-dataformat-yaml</artifactId>
		</dependency>
		<dependency>
			<groupId>com.google.guava</groupId>
			<artifactId>guava</artifactId>
			<version>${guava.version}</version>
		</dependency>
		<dependency>
			<groupId>com.github.zafarkhaja</groupId>
			<artifactId>java-semver</artifactId>
			<version>${semver.version}</version>
		</dependency>
		<dependency>
			<groupId>org.apache.commons</groupId>
			<artifactId>commons-compress</artifactId>
			<version>${commons-compress.version}</version>
		</dependency>

		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<optional>true</optional>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-configuration-processor</artifactId>
			<optional>true</optional>
		</dependency>
	</dependencies>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.springframework.cloud</groupId>
				<artifactId>spring-cloud-dependencies</artifactId>
				<version>${spring-cloud.version}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<profiles>
		<profile>
			<id>linux-profile</id>
			<activation>
				<os>
					<family>unix</family>
				</os>
			</activation>
			<properties>
				<helm.downloadUrl>https://storage.googleapis.com/kubernetes-helm/helm-v2.9.1-linux-amd64.tar.gz</helm.downloadUrl>
			</properties>
		</profile>
		<profile>
			<id>mac-profile</id>
			<activation>
				<os>
					<family>mac</family>
				</os>
			</activation>
			<properties>
				<helm.downloadUrl>https://storage.googleapis.com/kubernetes-helm/helm-v2.9.1-darwin-amd64.tar.gz</helm.downloadUrl>
			</properties>
		</profile>
	</profiles>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-deploy-plugin</artifactId>
				<configuration>
					<skip>true</skip>
				</configuration>
			</plugin>
			<plugin>
				<groupId>io.fabric8</groupId>
				<artifactId>docker-maven-plugin</artifactId>
				<version>${docker.plugin.version}</version>
				<configuration>
					<images>
						<image>
							<name>deviceinsight/bumbershoot</name>
							<build>
								<from>${docker.baseImage}</from>
								<tags>
									<tag>${project.version}</tag>
								</tags>
								<assembly>
									<targetDir>/opt/application</targetDir>
									<descriptorRef>artifact</descriptorRef>
								</assembly>
								<workdir>/opt/application</workdir>
								<cmd>java -jar $JAVA_OPTIONS /opt/application/${project.build.finalName}.jar</cmd>
							</build>
						</image>
					</images>
				</configuration>
				<executions>
					<execution>
						<id>docker-build</id>
						<goals>
							<goal>build</goal>
						</goals>
					</execution>
					<execution>
						<id>docker-push</id>
						<goals>
							<goal>push</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>com.kiwigrid</groupId>
				<artifactId>helm-maven-plugin</artifactId>
				<version>${helm.plugin.version}</version>
				<configuration>
					<chartDirectory>${project.build.directory}/helm-charts</chartDirectory>
					<chartVersion>${project.version}</chartVersion>
					<uploadRepoStable>
						<name>stable-repo</name>
						<url>${helm.repository.url}</url>
						<type>${helm.repository.type}</type>
					</uploadRepoStable>
					<helmExtraRepos>
						<helmRepo>
							<name>appscode</name>
							<url>https://charts.appscode.com/stable/</url>
						</helmRepo>
					</helmExtraRepos>
				</configuration>
				<executions>
					<execution>
						<id>helm-package</id>
						<phase>package</phase>
						<goals>
							<goal>init</goal>
							<goal>dependency-build</goal>
							<goal>package</goal>
						</goals>
					</execution>
					<!-- 
					<execution>
						<id>helm-upload</id>
						<phase>deploy</phase>
						<goals>
							<goal>helm:upload</goal>
						</goals>
					</execution>
				 	-->
				</executions>
			</plugin>
		</plugins>
		<resources>
			<resource>
				<directory>src/main/helm</directory>
				<filtering>true</filtering>
				<targetPath>${project.build.directory}/helm-charts</targetPath>
			</resource>
		</resources>
	</build>
</project>
