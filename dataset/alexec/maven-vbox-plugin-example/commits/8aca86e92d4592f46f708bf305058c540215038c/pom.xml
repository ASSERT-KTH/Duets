<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.alexecollins.vbox</groupId>
    <artifactId>maven-vbox-plugin-example</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>war</packaging>

    <description>An example of using maven-vbox-plugin.</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <build>
        <finalName>${project.artifactId}</finalName>
        <plugins>
            <plugin>
                <groupId>com.alexecollins.vbox</groupId>
                <artifactId>vbox-maven-plugin</artifactId>
                <version>1.0.0-SNAPSHOT</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>clean</goal>
                            <goal>provision</goal>
                            <goal>start</goal>
                            <goal>stop</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>rpm-maven-plugin</artifactId>
              <version>2.1-alpha-3-SNAPSHOT</version>
                <configuration>
                  <group>Alex Collins</group>
                    <copyright>-</copyright>
                    <targetOS>linux</targetOS>
                    <targetVendor>redhat</targetVendor>
                    <mappings>
                        <mapping>
                          <directory>/usr/share/tomcat6/webapps/${project.artifactId}</directory>
                            <sources>
                                <source>
                                  <location>target/${project.artifactId}</location>
                                </source>
                            </sources>
                        </mapping>
                    </mappings>
                  <requires>
                      <require>tomcat6</require>
                  </requires>
                </configuration>
              <executions>
                  <execution>
                      <goals>
                          <goal>rpm</goal>
                      </goals>
                  </execution>
              </executions>
            </plugin>

            <plugin>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>1.7</version>
                <executions>
                    <execution>
                        <id>make-files-unix</id>
                        <phase>process-resources</phase>
                        <goals><goal>run</goal></goals>
                        <configuration>
                            <target>
                                <fixcrlf srcdir="src/main/vbox/app1/floppy0" eol="unix" />
                            </target>
                        </configuration>
                    </execution>
                    <execution>
                        <id>deploy-rpm-to-vbox</id>
                        <phase>pre-integration-test</phase>
                        <goals><goal>run</goal></goals>
                        <configuration>
                            <target>
                                <!-- note we force trust as new VMs will get new MAC etc. -->
                                <scp todir="root@192.168.56.2:/root" password="123456" trust="true">
                                    <fileset dir="${project.build.directory}/rpm/${project.artifactId}/RPMS/noarch"/>
                                </scp>
                                <sshexec host="192.168.56.2" username="root" password="123456" trust="true"
                                         command="yum -y install *.rpm"/>
                            </target>
                        </configuration>
                    </execution>
                </executions>
                <dependencies>
                    <dependency>
                        <groupId>org.apache.ant</groupId>
                        <artifactId>ant-jsch</artifactId>
                        <version>1.8.4</version>
                    </dependency>
                </dependencies>
            </plugin>
            <plugin>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>2.12</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <dependencies>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.10</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.jsoup</groupId>
            <artifactId>jsoup</artifactId>
            <version>1.6.1</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

</project>