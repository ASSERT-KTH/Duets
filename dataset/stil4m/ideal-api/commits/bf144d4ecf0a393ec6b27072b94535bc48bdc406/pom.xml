<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <packaging>jar</packaging>
    <version>1.0-SNAPSHOT</version>
    <groupId>nl.stil4m</groupId>
    <artifactId>payment-interceptor</artifactId>
    <name>Payment Interceptor</name>
    <properties>
        <org.springframework-spring-webmvc-version>3.1.2.RELEASE</org.springframework-spring-webmvc-version>
        <org.springframework.security-version>3.1.2.RELEASE</org.springframework.security-version>
        <cglib-version>2.2.2</cglib-version>
        <org.codehaus.jackson-jackson-mapper-asl-version>1.9.7</org.codehaus.jackson-jackson-mapper-asl-version>
        <javax.inject-version>1</javax.inject-version>
        <thymeleaf.version>2.0.8</thymeleaf.version>
        <org.springframework.data-spring-data-mongodb-version>1.1.0.RELEASE</org.springframework.data-spring-data-mongodb-version>
        <junit-version>4.10</junit-version>
        <org.mockito-mockito-all-version>1.9.0</org.mockito-mockito-all-version>
        <log4j.version>1.2.17</log4j.version>
        <slf4j.version>1.6.6</slf4j.version>
        <jetty.version>8.0.3.v20111011</jetty.version>
        <jetty.servlet.version>3.0.20100224</jetty.servlet.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    </properties>

    <dependencies>

        <!-- Testing -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>${junit-version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-all</artifactId>
            <version>${org.mockito-mockito-all-version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.apache.httpcomponents</groupId>
            <artifactId>httpclient</artifactId>
            <version>4.2.1</version>
            <scope>compile</scope>
        </dependency>

        <!-- Utils -->
        <dependency>
            <groupId>com.google.guava</groupId>
            <artifactId>guava</artifactId>
            <version>12.0</version>
        </dependency>

        <!-- The normal validator is not compatible with spring -->
        <dependency>
            <groupId>org.hibernate</groupId>
            <artifactId>hibernate-validator</artifactId>
            <version>4.2.0.Final</version>
        </dependency>

    </dependencies>

    <build>
        <plugins>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>2.3.2</version>
                <configuration>
                    <source>1.6</source>
                    <target>1.6</target>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <repositories>

        <repository>
            <id>spring-release</id>
            <name>Spring Maven Release Repository</name>
            <url>http://repo.springsource.org/libs-release</url>
        </repository>

        <!-- Only for spring data -->
        <repository>
            <id>spring-snapshot</id>
            <name>Spring Maven SNAPSHOT Repository</name>
            <url>http://repo.springsource.org/libs-snapshot</url>
        </repository>
        
    </repositories>

</project>
