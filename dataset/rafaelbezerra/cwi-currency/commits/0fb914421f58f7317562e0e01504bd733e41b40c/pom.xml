<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>rafaelbezerra.com</groupId>
	<artifactId>cwi-currency</artifactId>
	<version>0.0.1-SNAPSHOT</version>

	<name>Currency CWI</name>
	<description>Currency CWI Test</description>

	<developers>
		<developer>
			<id>rafaelbezerra</id>
			<name>Rafael Gustavo Bezerra</name>
			<email>rafaelgustavob@hotmail.com</email>
			<organization>CWI</organization>
		</developer>
	</developers>

	<properties>
		<jdk.version>1.8</jdk.version>
	</properties>

	<build>
		<finalName>currency-${project.version}</finalName>
		<plugins>
			<!-- download source code in Eclipse, best practice -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-eclipse-plugin</artifactId>
				<version>2.9</version>
				<configuration>
					<downloadSources>true</downloadSources>
					<downloadJavadocs>true</downloadJavadocs>
				</configuration>
			</plugin>

			<!-- Set a JDK compiler level -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>2.3.2</version>
				<configuration>
					<source>${jdk.version}</source>
					<target>${jdk.version}</target>
				</configuration>
			</plugin>

			<!-- Make this jar executable -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<configuration>
					<archive>
						<manifest>
							<!-- Jar file entry point -->
							<mainClass>com.rafaelbezerra.cwi.currency.Usage</mainClass>
						</manifest>
					</archive>
				</configuration>
			</plugin>

			<!-- Run JUnit tests -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>2.19.1</version>
				<dependencies>
					<dependency>
						<groupId>org.apache.maven.surefire</groupId>
						<artifactId>surefire-junit47</artifactId>
						<version>2.19.1</version>
					</dependency>
				</dependencies>
			</plugin>

			<!-- Build jar archive source -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-source-plugin</artifactId>
				<version>3.0.1</version>
				<configuration>
					<outputDirectory>/target</outputDirectory>
					<finalName>currency-sources-${project.version}</finalName>
					<attach>false</attach>
				</configuration>
			</plugin>

		</plugins>
	</build>

</project>