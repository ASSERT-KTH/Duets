<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                      http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>com.holisticon.jdk9</groupId>
	<artifactId>http2-jigsaw-showcase</artifactId>
	<version>1.0</version>

	<properties>
		<java.version>1.9</java.version>
		<additionalRuntimeArgLine>-addmods java.xml.bind</additionalRuntimeArgLine>
	</properties>

	<build>
		<plugins>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>properties-maven-plugin</artifactId>
				<version>1.0.0</version>
				<executions>
					<execution>
						<phase>initialize</phase>
						<goals>
							<goal>read-project-properties</goal>
						</goals>
						<configuration>
							<files>
								<file>dev.properties</file>
							</files>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.5.1</version><!--$NO-MVN-MAN-VER$ -->
				<configuration>
					<verbose>true</verbose>
					<fork>true</fork>
					<executable>${jdk9.home}/bin/javac
					</executable>
					<compilerVersion>1.9</compilerVersion>
					<source>1.9</source>
					<target>1.9</target>
					<compilerArgs>
						<arg>-Werror</arg>
						<!-- <arg>-verbose</arg> <arg>-modulepath target/dependency</arg> <arg>-modulesourcepath 
							${project.build.sourceDirectory}</arg> <arg>-addmods ALL-MODULE-PATH</arg> -->
					</compilerArgs>
					<testCompilerArgs>
						<arg>-Werror</arg>
						<!-- <arg>-verbose</arg> <arg>-modulepath target/dependency</arg> <arg>-modulesourcepath 
							${project.build.sourceDirectory}</arg> <arg>-addmods ALL-MODULE-PATH</arg> -->
					</testCompilerArgs>
				</configuration>
			</plugin>
		</plugins>
	</build>

	<dependencies>
	</dependencies>
	<pluginRepositories>
		<pluginRepository>
			<id>apache.snapshots</id>
			<url>http://repository.apache.org/snapshots/</url>
		</pluginRepository>
	</pluginRepositories>
</project>